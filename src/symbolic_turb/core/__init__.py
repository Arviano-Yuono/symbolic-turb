from .compute_physics import (
    compute_anisotropy,
    compute_basis_tensor,
    compute_incompressibility,
    compute_invariants,
    compute_k_field,
    compute_rotation_rate,
    compute_strain_rate,
)
from .flow_structure import FlowData
from .tensor_operators import deviatoric, double_dot, matmul, trace, transpose
